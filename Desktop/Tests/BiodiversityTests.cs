using System;
using System.Threading.Tasks;
using FhiModel.Services;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Tests
{
    [TestClass]
    public class BiodiversityTests
    {
        //private static readonly String _speciesFile = @"..\..\TestData\species.csv";
        [TestMethod]
        public void ReadSpeciesTest()
        {
            /*
            var species = Species.Create(_speciesFile);
            SpeciesService.WriteSpecies(species);
            var another = SpeciesService.ReadSpeciesAsync();
            Assert.IsTrue(species.Count == another.Count);
            */
            

        }
    }
}